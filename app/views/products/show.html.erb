<h1>Product</h1>

<h3><%= @product.title %></h3>
<h3><%= @product.description %></h3>
<h3><%= @product.price %></h3>

<%= link_to 'Edit the product ', edit_product_path(@product) %>

<%= link_to 'Eliminar producto', product_path(@product), data: {
  turbo_method: :delete,
  turbo_confirm: '¿Estás seguro?'
} %>
